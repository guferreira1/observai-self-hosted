apiVersion: v1
kind: Service
metadata:
  name: observai-web
spec:
  selector:
    app: observai-web
  ports:
    - name: http
      port: {{ .Values.web.port }}
      targetPort: {{ .Values.web.port }}
