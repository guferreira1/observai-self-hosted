services:
  observai-api:
    environment:
      # OBSERVAI_ENV=local switches the Go logger to debug level.
      OBSERVAI_ENV: local

  observai-web:
    environment:
      NEXT_PUBLIC_APP_ENV: local
