services:
  observai-api:
    image: ${OBSERVAI_API_IMAGE}:${OBSERVAI_API_VERSION}
    container_name: observai-api
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    expose:
      - "8080"
    networks:
      - observai

  observai-web:
    image: ${OBSERVAI_WEB_IMAGE}:${OBSERVAI_WEB_VERSION}
    container_name: observai-web
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      - observai-api
    expose:
      - "3000"
    networks:
      - observai

  nginx:
    image: nginx:1.27-alpine
    container_name: observai-nginx
    restart: unless-stopped
    depends_on:
      - observai-web
      - observai-api
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - observai

  postgres:
    image: postgres:16-alpine
    container_name: observai-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - observai_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - observai

  redis:
    image: redis:7-alpine
    container_name: observai-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - observai_redis_data:/data
    networks:
      - observai

networks:
  observai:
    name: observai

volumes:
  observai_postgres_data:
  observai_redis_data:
