apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: observai
  namespace: observai
spec:
  rules:
    - host: observai.example.com
      http:
        paths:
          - path: /api/observai
            pathType: Prefix
            backend:
              service:
                name: observai-web
                port:
                  number: 3000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: observai-web
                port:
                  number: 3000
